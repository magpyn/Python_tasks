import avg_speed

running_distance = float(input("Ile km przebiegłeś? "))
running_time = float((input("Ile godzin Ci to zajęło? "))

average_speed = avg_speed.aver_speed(running_distance, running_time)
print(f"Twoja średnia prędkość biegu to {avgerage_speed} km/h")